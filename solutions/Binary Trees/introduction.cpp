#include <bits/stdc++.h>
#include <stack>
#include <algorithm>
using namespace std;

// Introduction & Representation

struct Node {
    int value;
    Node* left;
    Node* right;
    Node(int v, Node* l, Node* r) {
        value = v;
        left = l;
        right = r;
    }
};

int main() {
    return 0;
}